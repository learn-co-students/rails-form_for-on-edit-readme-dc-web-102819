<h3>Post Form</h3>

<%= form_for @post do |f|%>
	<%= f.label :title %>
	<%= f.text_area :title %>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<%= f.submit "Update Post" %>
<% end %>